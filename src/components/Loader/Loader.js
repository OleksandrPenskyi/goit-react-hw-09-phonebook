import Loader from 'react-loader-spinner';

const Loading = () => (
  <Loader
    className="loader"
    type="TailSpin"
    color="#00BFFF"
    height={200}
    width={200}
  />
);

export default Loading;
