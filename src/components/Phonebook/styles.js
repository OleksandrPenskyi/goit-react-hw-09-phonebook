import { makeStyles } from '@material-ui/core';

export const PhonebookStyles = makeStyles({
  wrapper: {
    margin: '0 auto',
    display: 'flex',
    flexDirection: 'column',
    padding: 10,
    width: 600,
  },
});
