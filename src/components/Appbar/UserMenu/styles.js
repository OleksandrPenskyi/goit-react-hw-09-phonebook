import { makeStyles } from '@material-ui/core';

export const UserMenuStyles = makeStyles({
  section: {
    display: 'flex',
    alignItems: 'center',
  },

  btn: {
    marginRight: 20,
  },
  text: {
    marginRight: 20,
  },
});
